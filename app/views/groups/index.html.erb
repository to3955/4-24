<div class="container px-5 px-sm-0">
  <div class="row">

    <div class="col-md-3">
     <%= render 'books/list', user: @user, book: @book %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Groups</h2>
      <%= render 'index', groups: @groups %>
    </div>

    <div><%= @groups.map { |group| group.users.count }.sum %></div>

  </div>
</div>